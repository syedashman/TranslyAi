// One definition of the Log in / Sign up buttons, shared by the top-right header and the sidebar card so they always match.
// They are the app's standard button classes (see the button system in index.css): neutral secondary + green primary, small size.
export const CTA_LOGIN = 'btn btn-secondary btn-sm';
export const CTA_SIGNUP = 'btn btn-primary btn-sm';
